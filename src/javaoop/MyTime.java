package javaoop;

//对时间进行操作
public class MyTime {

	public static void main(String[] args) {
		MyTimeClass now = new MyTimeClass();
		now.setHour(9);
		now.setMin(9);
		now.setSec(9);
		now.display();
	}
}